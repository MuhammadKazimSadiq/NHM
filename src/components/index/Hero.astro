---
import Button from "../shared/Button.astro"
import NavLink from "../shared/NavLink.astro"
---

<header class="hero relative min-h-[85vh] md:min-h-screen bg-cover bg-center bg-[url(/src/images/index/hero.jpg)] text-white">
  <div class="hero-overlay flex min-h-[85vh] md:min-h-screen flex-col bg-[linear-gradient(180deg,rgba(0,0,0,0.65),rgba(0,0,0,0.35),rgba(0,0,0,0.8))]">

    <nav class="nav mx-auto flex max-w-7xl items-center justify-between p-5 gap-12">
      <div class="logo font-heading text-lg font-medium tracking-wider">
        NHM Travels
      </div>

      <ul class="nav-links m-0 hidden list-none gap-6 lg:flex">
        <NavLink href="#intro">About</NavLink>
        <NavLink href="#destinations">Destinations</NavLink>
        <NavLink href="#experiences">Experiences</NavLink>
        <NavLink href="#services">Services</NavLink>
        <NavLink href="#contact">Contact</NavLink>
      </ul>
    </nav>

    <div class="hero-content container flex flex-1 flex-col justify-center pb-20">
      <p class="hero-kicker mb-8 text-base uppercase tracking-[0.16em] text-[#f3e3c6]">
        Discover Iran with
      </p>

      <h1
        class="font-heading mb-3 text-[clamp(2.6rem,4vw,3.6rem)] drop-shadow-[0_12px_32px_rgba(0,0,0,0.9)]">
        Timeless Journeys Through Iran
      </h1>

      <p class="hero-subtitle mb-7 max-w-130 text-base text-gray-100">
        From ancient cities to desert skies, experience the depth of Persian
        culture with curated itineraries by NHM Travels.
      </p>

      <Button variant="primary" href="#contact">
        Book Now
      </Button>
    </div>
  </div>
</header>