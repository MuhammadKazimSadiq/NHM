---
import Button from "../shared/Button.astro"
---

<style>
.logo {
  font-family: var(--font-heading);
}

.nav-links a::after {
  content: "";
  position: absolute;
  bottom: -0.25rem;
  left: 0;
  width: 0;
  height: 2px;
  background-color: var(--color-accent);
  transition: 0.2s ease;
}

.nav-links a:hover::after {
  width: 100%;
}

.hero-kicker {
  letter-spacing: 0.16em;
}

.hero h1 {
  font-family: var(--font-heading);
  font-size: clamp(2.6rem, 4vw, 3.6rem);
  margin: 0 0 0.8rem;
  text-shadow: 0 12px 32px rgba(0, 0, 0, 0.9);
}

.hero-subtitle {
  max-width: 520px;
  font-size: 0.98rem;
  color: #f4f4f4;
  margin-bottom: 1.8rem;
}
</style>

<header class="hero min-h-screen text-white relative bg-cover bg-center bg-[url(/src/images/index/hero.jpg)]">
    <div class="hero-overlay min-h-screen flex flex-col bg-[linear-gradient(180deg,rgba(0,0,0,0.65),rgba(0,0,0,0.35),rgba(0,0,0,0.8))]">
        <nav class="nav flex justify-between items-center p-5 my-0 mx-auto max-w-7xl">
        <div class="logo font-medium tracking-wider text-lg">NHM Travels</div>
        <ul class="nav-links list-none flex gap-6 m-0">
            <li><a href="#intro" class="text-gray-100 font-medium relative text-base">About</a></li>
            <li><a href="#destinations" class="text-gray-100 font-medium relative text-base">Destinations</a></li>
            <li><a href="#experiences" class="text-gray-100 font-medium relative text-base">Experiences</a></li>
            <li><a href="#services" class="text-gray-100 font-medium relative text-base">Services</a></li>
            <li><a href="#contact" class="text-gray-100 font-medium relative text-base">Contact</a></li>
        </ul>
        </nav>

        <div class="hero-content container flex flex-col justify-center flex-1 pb-20">
        <p class="hero-kicker text-base uppercase text-[#f3e3c6] mb-8">Discover Iran with</p>
        <h1>Timeless Journeys Through Iran</h1>
        <p class="hero-subtitle">
            From ancient cities to desert skies, experience the depth of Persian
            culture with curated itineraries by NHM Travels.
        </p>
        <Button variant="primary" href="#contact">Book Now</Button>
        </div>
    </div>
</header>